import React from "react";
import { View, Text, Image, StyleSheet } from "react-native";

const SingleBusiness = ({ listing }) => {
    return <Text>{listing.name}</Text>;
};

const style = StyleSheet.create({});

export default SingleBusiness;
